{
  "buildCommand": "npm install --prefix Frontend-Vercel && npm run build --prefix Frontend-Vercel",
  "outputDirectory": "Frontend-Vercel/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

